// Compatibility export for existing callers. New code should import from the
// node registry so manual and AI-created nodes always use the same defaults.
export { NODE_SIZES as nodeDimensions } from "./nodeRegistry";
